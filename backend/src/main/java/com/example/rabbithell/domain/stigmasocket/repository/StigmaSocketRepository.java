package com.example.rabbithell.domain.stigmasocket.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.example.rabbithell.domain.stigmasocket.entity.StigmaSocket;

public interface StigmaSocketRepository extends JpaRepository<StigmaSocket, Long> {
}
